name: Build Binaries
on:
  push:
  workflow_dispatch: 



jobs:     
  build:
    runs-on: macos-latest
    strategy:
      matrix:
        os: [darwin, linux, windows]
        arch: [amd64, arm64]
        exclude:
          - os: windows
            arch: arm64
    env:
      BUILD_OS: ${{ matrix.os }}
      BUILD_ARCH: ${{ matrix.arch }}
    steps:
    - run: echo "$BUILD_OS $BUILD_ARCH"
    # - name: Copy Repository Contents
    #   uses: actions/checkout@master
    # - name: Build Binary
    #   run: ./_action_files/mac.sh
    # - name: Create Release
    #   id: create_release
    #   uses: actions/create-release@v1
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   with:
    #     tag_name: ${{ github.sha }}-${{ matrix.os }}-${{ matrix.arch }}
    #     release_name: Mac OSX Release ${{ github.ref }}
    #     body: OSX binary for latest MacOSX.
    # - name: Upload Release Asset
    #   id: upload-release-asset 
    #   uses: actions/upload-release-asset@v1
    #   env:
    #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #   with:
    #     upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps 
    #     asset_path: ~/go/bin/hugo
    #     asset_name: hugo
    #     asset_content_type: application/unknown
